import { TelephoneFormatPipe } from './telephone-format.pipe';

describe('TelephoneFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new TelephoneFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
