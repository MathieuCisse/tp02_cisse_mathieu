import { InputInvalidDirective } from './input-invalid.directive';

describe('InputInvalidDirective', () => {
  it('should create an instance', () => {
    const directive = new InputInvalidDirective();
    expect(directive).toBeTruthy();
  });
});
